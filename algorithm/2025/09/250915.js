// 8 kyu Training JS #5: Basic data types--Object
// my solution
const animal = (obj) => "This " + obj.color + " " + obj.name + " has " + obj.legs + " legs.";


/**
 * best solution
 * `を使って並ぶ方が見やすい感がある
 */
// cosnt animal = (obj) => `This ${obj.color} ${obj.name} has ${obj.legs} lengs.`;
